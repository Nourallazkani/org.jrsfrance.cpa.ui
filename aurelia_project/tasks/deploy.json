{
  "name": "deploy",
  "description": "Deploy to production environment",
  "flags": [
    {
      "name": "buckets",
      "description": "set the s3 buckets (if many, each of them must be separated by a comma)",
      "type": "string"
    }
  ]
}